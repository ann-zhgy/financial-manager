spring:
  application:
    name: provider-deal-group-service

mybatis:
  type-aliases-package: cn.ann.financial.manager.provider.deal.group.domain
  mapperLocations: classpath:mapper/*.xml

dubbo:
  scan:
    base-packages: cn.ann.financial.manager.provider.deal.group.service
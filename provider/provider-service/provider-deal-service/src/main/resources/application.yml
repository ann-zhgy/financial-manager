spring:
  application:
    name: provider-deal-service

mybatis:
  type-aliases-package: cn.ann.financial.manager.provider.deal.domain
  mapperLocations: classpath:mapper/*.xml

dubbo:
  scan:
    base-packages: cn.ann.financial.manager.provider.deal.service

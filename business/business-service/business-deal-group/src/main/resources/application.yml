spring:
  application:
    name: business-deal-group
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss

security:
  oauth2:
    resource:
      id: backend-resources-deal-group

dubbo:
  scan:
    base-packages: cn.ann.financial.manager.business.deal.group.controller

server:
  port: 9506
